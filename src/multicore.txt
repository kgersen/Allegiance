Server side:

	extend CFLServer to include name/core
	
	lobby/messageslc.h: LOGON_LOBBY to include server name
	server/FedSrv.cpp: ConnectToLobby
	lobby/server.cpp: LobbyServerSite::OnNewConnection
	
	FM_L_CREATE_MISSION_REQ in server\fslobby.cpp: retrieve core name.

	on server: PAUSE or LOGOFF_LOBBY or missed HEARTBEAT: broadcast to clients the info ( do like CFLServer::Pause)
Client side:

	add new message SERVERINFO to advertise server names.
	
	lobby/messagelc.h: CREATE_MISSION_REQ add server name and core name
	lobby/client.cpp: FM_C_CREATE_MISSION_REQ : handle server name and core name